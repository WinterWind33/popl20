{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "[CMAKE] Configure all",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/build",
            },
            "command": "cmake",
            "args": [
                ".."
            ],
            "group": "build"
        }
    ]
}
